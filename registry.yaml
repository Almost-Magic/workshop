# The Workshop — Canonical Service Registry
# This file is the single source of truth for all AMTL applications.
# Do NOT hardcode service data in Python — always read from here.
#
# Format: AMTL-WKS-TDD-1.0 §4
# Updated: 23 February 2026

services:

  # ── CORE ───────────────────────────────────────────────
  - id: elaine
    name: ELAINE
    description: AI Chief of Staff
    group: Core
    ports: { api: 5000 }
    start_command: "python app.py"
    working_dir: "../CK_Elaine"
    health_endpoint: "http://localhost:5000/api/health"
    dependencies: [supervisor, ollama]
    ghost: false

  - id: workshop
    name: The Workshop
    description: AMTL Ecosystem Launchpad
    group: Core
    ports: { api: 5003 }
    start_command: "python app.py"
    working_dir: "."
    health_endpoint: "http://localhost:5003/api/health"
    dependencies: []
    ghost: false

  - id: supervisor
    name: Supervisor
    description: GPU and model manager
    group: Core
    ports: { api: 9000 }
    start_command: "python app.py"
    working_dir: "../CK_Supervisor"
    health_endpoint: "http://localhost:9000/api/health"
    dependencies: [ollama]
    ghost: false

  - id: foreperson
    name: Foreperson
    description: Quality auditor
    group: Core
    ports: { api: 9100 }
    start_command: "python app.py"
    working_dir: "../CK_Foreperson"
    health_endpoint: "http://localhost:9100/api/health"
    dependencies: []
    ghost: false

  - id: inspector
    name: Inspector
    description: Code quality monitor
    group: Core
    ports: { api: 9200 }
    start_command: "python app.py"
    working_dir: "../CK_Inspector"
    health_endpoint: "http://localhost:9200/api/health"
    dependencies: []
    ghost: false

  # ── CK — PERSONAL ──────────────────────────────────────
  - id: ck-writer
    name: CK Writer
    description: Long-form writing tool
    group: CK — Personal
    ports: { api: 5004 }
    start_command: "python app.py"
    working_dir: "../CK_Writer"
    health_endpoint: "http://localhost:5004/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: learning-assistant
    name: Learning Assistant
    description: Study and learning tool
    group: CK — Personal
    ports: { api: 5002 }
    start_command: "python app.py"
    working_dir: "../CK_LearningAssistant"
    health_endpoint: "http://localhost:5002/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: costanza
    name: Costanza
    description: Thinking frameworks
    group: CK — Personal
    ports: { api: 5014 }
    start_command: "python app.py"
    working_dir: "../CK_Costanza"
    health_endpoint: "http://localhost:5014/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: author-studio
    name: Author Studio
    description: Publishing and author tools
    group: CK — Personal
    ports: { api: 5007 }
    start_command: "python app.py"
    working_dir: "../CK_AuthorStudio"
    health_endpoint: "http://localhost:5007/api/health"
    dependencies: [supervisor]
    ghost: false

  # ── CK — BUSINESS ──────────────────────────────────────
  - id: ripple-crm
    name: Ripple CRM
    description: Customer relationship management
    group: CK — Business
    ports: { api: 5001 }
    start_command: "python app.py"
    working_dir: "../CK_RippleCRM"
    health_endpoint: "http://localhost:5001/api/health"
    dependencies: []
    ghost: false

  - id: junk-drawer
    name: Junk Drawer
    description: Miscellaneous tools and scratch space
    group: CK — Business
    ports: { ui: 3005, api: 5005 }
    start_command: "python app.py"
    working_dir: "../CK_JunkDrawer"
    health_endpoint: "http://localhost:5005/api/health"
    dependencies: []
    ghost: false

  - id: opp-hunter
    name: Opp Hunter
    description: Opportunity discovery
    group: CK — Business
    ports: { api: 5006 }
    start_command: "python app.py"
    working_dir: "../CK_OppHunter"
    health_endpoint: "http://localhost:5006/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: the-ledger
    name: The Ledger
    description: Financial tracking
    group: CK — Business
    ports: { api: 5020 }
    start_command: "python app.py"
    working_dir: "../CK_TheLedger"
    health_endpoint: "http://localhost:5020/api/health"
    dependencies: []
    ghost: false

  - id: genie
    name: Genie
    description: Automation and workflow
    group: CK — Business
    ports: { api: 8000 }
    start_command: "python app.py"
    working_dir: "../CK_Genie"
    health_endpoint: "http://localhost:8000/api/health"
    dependencies: [supervisor]
    ghost: false

  # ── INTELLIGENCE ───────────────────────────────────────
  - id: peterman
    name: Peterman
    description: SEO and LLM ranking service
    group: Intelligence
    ports: { api: 5008 }
    start_command: "python app.py"
    working_dir: "../CK_Peterman"
    health_endpoint: "http://localhost:5008/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: identity-atlas
    name: Identity Atlas
    description: Identity and persona mapping
    group: Intelligence
    ports: { api: 5009 }
    start_command: "python app.py"
    working_dir: "../CK_IdentityAtlas"
    health_endpoint: "http://localhost:5009/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: digital-sentinel
    name: Digital Sentinel
    description: Cybersecurity scanner
    group: Intelligence
    ports: { api: 8500 }
    start_command: "python app.py"
    working_dir: "../CK_DigitalSentinel"
    health_endpoint: "http://localhost:8500/api/health"
    dependencies: [supervisor]
    ghost: false

  # ── MARKETING ──────────────────────────────────────────
  - id: spark
    name: Spark
    description: Marketing and campaign tools
    group: Marketing
    ports: { api: 5011 }
    start_command: "python app.py"
    working_dir: "../CK_Spark"
    health_endpoint: "http://localhost:5011/api/health"
    dependencies: [supervisor]
    ghost: false

  # ── OPERATIONS ─────────────────────────────────────────
  - id: processlens
    name: ProcessLens
    description: Process analysis and mapping
    group: Operations
    ports: { api: 5016 }
    start_command: "python app.py"
    working_dir: "../CK_ProcessLens"
    health_endpoint: "http://localhost:5016/api/health"
    dependencies: [supervisor]
    ghost: false

  - id: signal
    name: Signal
    description: Notifications and alerts
    group: Operations
    ports: { api: 8420 }
    start_command: "python app.py"
    working_dir: "../CK_Signal"
    health_endpoint: "http://localhost:8420/api/health"
    dependencies: []
    ghost: false

  # ── GHOST (PLANNED) ────────────────────────────────────
  - id: sophia
    name: Sophia
    description: Intelligence Engine
    group: Ghost
    ports: { api: 5010 }
    ghost: true
    ghost_icon: "✦"
    ghost_eta: "Q2 2026"

  - id: amtl-tts
    name: AMTL TTS
    description: Voice synthesis studio
    group: Ghost
    ports: { api: 5015 }
    ghost: true
    ghost_icon: "✦"
    ghost_eta: "Q2 2026"

  - id: dhamma-mirror
    name: Dhamma Mirror
    description: Reflective practice tool
    group: Ghost
    ports: { api: 5019 }
    ghost: true
    ghost_icon: "✦"
    ghost_eta: "Q3 2026"

  - id: after-i-go
    name: After I Go
    description: Legacy and continuity planning
    group: Ghost
    ports: { api: 5173 }
    ghost: true
    ghost_icon: "✦"
    ghost_eta: "Q3 2026"
